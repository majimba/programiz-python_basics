#!/usr/bin/python3

# Find the Square Root
# we can use the ** operator (exponent operator) to find the square root

number = 25

square_root = number ** (1/2)
print('square root of', number, 'is', square_root)
